# Nixpacks configuration for Railway
# Installs system dependencies needed for the app

[phases.setup]
nixPkgs = ["tesseract", "python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"

